version: '2' 

services:
  web:
    image: tmarkovic/docker-angular-cli
    ports:
      - "4200:4200"

  api:
    image: tmarkovic/docker-express
    ports:
      - "3000:3000"
    links: 
        - db

  db:
    image: mongo
    ports:
      - "27017:27017"